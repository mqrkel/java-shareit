server:
  port: 9090

spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
        show_sql: true
  sql:
    init:
      mode: never
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:shareit
    username: shareit
    password: shareit